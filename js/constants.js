// 設定タブで扱う値の既定値。**このファイルが唯一の出所**。
//
// 設定値は IndexedDB へ永続化しない。起動のたびにここの値から始まり、
// 設定タブでの変更はその起動中（リロードするまで）だけ有効になる。
// 端末に保存した古い値が新しい既定値を覆い隠すことがないため、
// 運用方針を変えたいときは、このファイルを書き換えて配布すれば全端末に等しく効く。
// （sw.js の APP_VERSION も併せて改めること。そうしないと配信済みの端末へ届かない）
//
// ここに置くのは「設定タブから変更できる値」だけ。画面に出さないモジュール定数
// （収束判定の holdSec / 許容量、生NMEA の行数上限など）は各モジュール側に置く
// （docs/algospec-202607.md 3-4）。
export const DEFAULT_SETTINGS = {
  // ---- 測位・記録 ----
  uere: 5, // HDOP×UERE 概算用 [m]。GST が無いときの水平精度推定に使う
  autoStop: true, // 収束（中心・DRMS横ばい）による自動停止
  deviceGnss: true, // 記録中に端末内蔵GNSSも取得して DRMS を比較する（仕様 4-8）
  saveRawNmea: true, // 記録中の生NMEA行もそのまま保存する（後から別ツールで再解析するため）
  minSec: 30, // 記録の最低収集時間 [秒]。見通しの良い場所での所要時間はこの値でほぼ決まる
  maxSec: 180, // 記録の上限時間 [秒]（タイムアウト）。山間部など未収束の環境で粘れる長さ。0=無制限
  maxEpochs: 300, // 記録の上限エポック数。1Hz では 180 秒に届かないため実質は保険。0=無制限

  // ---- 地図 ----
  mapType: 'std', // 'std' | 'pale' | 'photo'
  trackEnabled: true, // ライブ軌跡の表示
};
